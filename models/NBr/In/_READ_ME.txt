Contains data/files that aren't used directly in any software. E.g. IDF files that are used to create MF input files.

Only the first version of each file is tracked via DVC. The rest can be recreated with scripts in C:\OD\WS_Mdl\code\PrP (the scripts are tracked by Git).