- Prefer capitalization-based abbreviation style in code variable names (e.g., `GDF`, `DA`, `CRS`, first letter of each word capitalized in abbreviations).
- Avoid mixed abbreviation/lowercase helper names like `gdf_clip`; prefer styles like `GDF_Clip`.

- Prefer concise, final-only responses: skip progress narration and redundant status text unless blocked or a clarifying question is required.
